# AI Learn - Интерактивный AI-ассистент

Терминальное приложение на Kotlin для интерактивного общения с AI через различные LLM провайдеры (Perplexity AI, OpenAI, Google и др.). Приложение построено на модульной, масштабируемой архитектуре с четким разделением ответственности.

## 🚀 Возможности

- **Модульная архитектура** - легко добавлять новые LLM провайдеры и интерфейсы
- **Интерактивный диалог** с поддержкой истории сообщений
- **Автоматическая суммаризация** - автоматическое сжатие истории диалога при превышении лимита токенов
- **Опциональное персистентное хранение истории** - возможность сохранения истории диалога между перезапусками приложения (JSON или SQLite, по умолчанию отключено)
- **Гибкая конфигурация** - через переменные окружения, файл конфигурации или BuildConfig
- **Отслеживание токенов** - автоматический расчет стоимости использования API
- **Типизированная обработка ошибок** через sealed classes
- **Чистая архитектура** - разделение на слои: Core, API, Frontend
- **Легкая расширяемость** - добавление новых провайдеров и интерфейсов без изменения ядра
- **SOLID принципы** - код следует лучшим практикам проектирования
- **🔧 MCP интеграция** - поддержка Model Context Protocol для вызова инструментов
- **🔄 Автоматический вызов инструментов** - LLM может запрашивать инструменты, которые автоматически выполняются
- **⚡ Итеративный цикл** - автоматическое выполнение инструментов и получение финального ответа
- **🌐 Множественные MCP серверы** - поддержка нескольких серверов с разными типами транспорта (SSE и StreamableHttp)
- **🎯 Умная маршрутизация** - инструменты автоматически вызываются в правильном сервере

## 📋 Требования

- **Java 21** или выше
- **Gradle 8.0** или выше

## 🔧 Установка

1. Клонируйте репозиторий или скачайте проект:
   ```bash
   git clone <repository-url>
   cd aiLearn
   ```

2. Настройте API ключ одним из способов:

   **Способ 1: Переменные окружения (рекомендуется)**
   ```bash
   export AILEARN_API_KEY=ваш_api_ключ
   ```

   **Способ 2: Файл конфигурации**
   Скопируйте файл-пример и настройте его:
   ```bash
   cp ailearn.config.properties.example ailearn.config.properties
   # Отредактируйте файл и добавьте ваш API ключ
   ```
   
   Или создайте файл `ailearn.config.properties` в корне проекта вручную:
   ```properties
   api.key=ваш_api_ключ
   api.url=https://api.perplexity.ai/chat/completions
   model=sonar
   max.tokens=500
   temperature=0.6
   system.prompt=Answers must be short and succinct
   dialog.end.marker=###END###
   price.per.million.tokens=1.0
   request.timeout.millis=60000
   use.message.history=false
   ```

   **Способ 3: BuildConfig (для сборки)**
   Создайте файл `gradle.properties` в корне проекта:
   ```properties
   kotlin.code.style=official
   perplexityApiKey=ваш_api_ключ
   ```

   > **Важно:** Файлы `gradle.properties` и `ailearn.config.properties` уже добавлены в `.gitignore` и не будут попадать в репозиторий.

## 🏃 Запуск

### Через Gradle

```bash
./gradlew run
```

### Сборка JAR файла

```bash
./gradlew build
```

Запуск собранного JAR:
```bash
java -jar build/libs/aiLearn-1.0-SNAPSHOT.jar
```

## 💻 Использование

После запуска программы:

1. Вы увидите приветственное сообщение
2. Введите ваш вопрос или сообщение
3. Ассистент ответит на основе контекста диалога (если включена история)
4. Для выхода в любой момент введите `exit` или `quit`
5. Если ответ содержит маркер `###END###`, диалог завершится автоматически

### Пример использования

```
Введите 'exit' или 'quit' для выхода в любой момент
temp is 0.6

Ввод: Привет! Расскажи о Kotlin

Kotlin — это современный язык программирования...

--- Token Usage ---
Prompt tokens: 45
Completion tokens: 120
Total tokens: 165
Price: $0.000165
------------------

Ввод: А что насчет корутин?

Корутины в Kotlin — это легковесные потоки...

--- Token Usage ---
Prompt tokens: 210
Completion tokens: 95
Total tokens: 305
Price: $0.000305
------------------

Ввод: exit
Завершение работы...
Программа завершена.
```

## 📁 Структура проекта

```
aiLearn/
├── src/main/kotlin/
│   ├── Main.kt                          # Точка входа приложения
│   ├── core/                            # Ядро приложения (бизнес-логика)
│   │   ├── domain/                      # Доменные модели
│   │   │   ├── Message.kt
│   │   │   ├── MessageRole.kt
│   │   │   ├── TokenUsage.kt
│   │   │   ├── ChatRequest.kt
│   │   │   └── ChatResponse.kt
│   │   ├── provider/                    # Интерфейс LLM провайдера
│   │   │   └── LlmProvider.kt
│   │   ├── conversation/                # Бизнес-логика
│   │   │   ├── ConversationManager.kt
│   │   │   ├── ConversationSummarizer.kt
│   │   │   ├── TokenCostCalculator.kt
│   │   │   ├── ToolCallingHandler.kt    # Управление циклом вызова инструментов
│   │   │   └── ToolRequestParser.kt      # Парсинг запросов инструментов
│   │   ├── mcp/                         # MCP доменные модели
│   │   │   └── McpModels.kt
│   │   ├── memory/                      # Персистентное хранение истории
│   │   │   ├── MemoryStore.kt           # Интерфейс хранилища
│   │   │   ├── JsonMemoryStore.kt      # Реализация на JSON
│   │   │   ├── SqliteMemoryStore.kt   # Реализация на SQLite
│   │   │   └── MemoryStoreFactory.kt   # Фабрика хранилищ
│   │   ├── config/                      # Управление конфигурацией
│   │   │   ├── AppConfig.kt
│   │   │   └── ConfigLoader.kt
│   │   └── utils/                       # Утилиты
│   │       └── AutoCloseableExtensions.kt
│   ├── api/                             # Реализации API
│   │   ├── provider/
│   │   │   └── ProviderFactory.kt       # Фабрика провайдеров
│   │   ├── perplexity/                  # Реализация Perplexity API
│   │   │   ├── PerplexityModels.kt
│   │   │   ├── PerplexityAdapter.kt
│   │   │   └── PerplexityProvider.kt
│   │   └── mcp/                         # Интеграция с MCP серверами
│   │       ├── McpConfig.kt             # Конфигурация MCP серверов и типов транспорта
│   │       ├── McpClient.kt             # Общий интерфейс для MCP клиентов
│   │       ├── McpSseClient.kt          # SSE клиент для MCP
│   │       ├── McpStreamableHttpClient.kt # StreamableHttp клиент для MCP
│   │       ├── McpServiceImpl.kt        # Реализация MCP сервиса
│   │       └── McpSseModels.kt          # Модели MCP протокола
│   └── frontend/                        # Интерфейсы пользователя
│       ├── Frontend.kt                  # Интерфейс фронтенда
│       └── cli/
│           └── CliFrontend.kt           # CLI реализация
├── build.gradle.kts                     # Конфигурация сборки
├── ARCHITECTURE.md                      # Документация архитектуры
└── README.md                            # Документация
```

## 🏗 Архитектура

Приложение построено на принципах **Clean Architecture** с четким разделением на слои:

### Слои архитектуры

1. **Core Domain Layer** (`core/`) - Бизнес-логика, независимая от внешних фреймворков
2. **API Layer** (`api/`) - Реализации LLM провайдеров (Perplexity, с возможностью добавления других)
3. **Frontend Layer** (`frontend/`) - Интерфейсы пользователя (CLI, с возможностью добавления GUI/HTTP API)
4. **Configuration Layer** (`core/config/`) - Централизованное управление конфигурацией

### Принципы проектирования

- **SOLID** - все принципы соблюдены
- **Dependency Inversion** - зависимости направлены внутрь (Frontend → Core → API)
- **Single Responsibility** - каждый класс имеет одну ответственность
- **Open/Closed** - открыто для расширения, закрыто для модификации

Подробнее см. [ARCHITECTURE.md](ARCHITECTURE.md)

## ⚙️ Конфигурация

### Переменные окружения

Приоритет конфигурации (от высшего к низшему):
1. Переменные окружения
2. Файл `ailearn.config.properties`
3. BuildConfig (из `gradle.properties`)
4. Значения по умолчанию

### Доступные переменные окружения

- `AILEARN_API_KEY` - API ключ (обязательно)
- `AILEARN_API_URL` - URL API endpoint
- `AILEARN_MODEL` - Название модели
- `AILEARN_MAX_TOKENS` - Максимальное количество токенов
- `AILEARN_TEMPERATURE` - Температура (0.0-2.0)
- `AILEARN_SYSTEM_PROMPT` - Системный промпт
- `AILEARN_DIALOG_END_MARKER` - Маркер завершения диалога
- `AILEARN_PRICE_PER_MILLION_TOKENS` - Цена за миллион токенов
- `AILEARN_REQUEST_TIMEOUT_MILLIS` - Таймаут запроса в миллисекундах
- `AILEARN_USE_MESSAGE_HISTORY` - Сохранять историю сообщений между перезапусками (true/false, по умолчанию: false)
- `AILEARN_SUMMARIZATION_TOKEN_THRESHOLD` - Порог токенов для запуска суммаризации (по умолчанию: 600)
- `AILEARN_SUMMARIZATION_MODEL` - Модель для суммаризации (по умолчанию: "sonar")
- `AILEARN_SUMMARIZATION_MAX_TOKENS` - Максимум токенов для запроса суммаризации (по умолчанию: 500)
- `AILEARN_SUMMARIZATION_TEMPERATURE` - Температура для суммаризации (по умолчанию: 0.3)
- `AILEARN_SUMMARIZATION_SYSTEM_PROMPT` - Системный промпт для запросов суммаризации
- `AILEARN_SUMMARIZATION_PROMPT` - Инструкция для суммаризации
- `AILEARN_MEMORY_STORE_TYPE` - Тип хранилища истории ("json" или "sqlite", по умолчанию: "json")
- `AILEARN_MEMORY_STORE_PATH` - Опциональный путь к файлу/базе данных хранилища (по умолчанию: текущая директория)

### Пример конфигурационного файла

Создайте `ailearn.config.properties`:

```properties
api.key=ваш_api_ключ
api.url=https://api.perplexity.ai/chat/completions
model=sonar
max.tokens=500
temperature=0.6
system.prompt=Answers must be short and succinct
dialog.end.marker=###END###
price.per.million.tokens=1.0
request.timeout.millis=60000
use.message.history=false
summarization.token.threshold=600
summarization.model=sonar
summarization.max.tokens=500
summarization.temperature=0.3
summarization.system.prompt=You are a helpful assistant that summarizes conversations concisely.
summarization.prompt=Please provide a brief summary of the conversation so far, capturing the key topics, questions, and answers discussed. Keep it concise and focused on the main points.
memory.store.type=json
memory.store.path=ailearn.history.json
```

### 💾 Управление сохранением истории

По умолчанию история диалога **не сохраняется между перезапусками** приложения (`use.message.history=false`). Это означает:

- ✅ Каждый запуск начинается с чистого состояния
- ✅ Файлы истории (JSON/SQLite) не создаются
- ✅ Меньше использования дискового пространства
- ✅ Больше конфиденциальности

**Чтобы включить сохранение истории между перезапусками:**

1. **Через переменную окружения:**
   ```bash
   export AILEARN_USE_MESSAGE_HISTORY=true
   ```

2. **Через конфигурационный файл:**
   ```properties
   use.message.history=true
   ```

Когда `use.message.history=true`:
- История диалога сохраняется автоматически после каждого сообщения
- При следующем запуске история автоматически загружается
- Можно выбрать тип хранилища: `json` (по умолчанию) или `sqlite`
- Можно указать путь к файлу через `memory.store.path`

## 🛠 Технологии

- **Kotlin 2.2.10** - Язык программирования
- **Ktor 3.3.2** - HTTP клиент
- **Kotlinx Serialization** - Сериализация JSON
- **Kotlin Coroutines** - Асинхронное программирование
- **SQLite JDBC 3.44.1.0** - Драйвер для SQLite базы данных
- **MCP Kotlin SDK 0.8.1** - Model Context Protocol SDK
- **Gradle** - Система сборки
- **SLF4J Simple** - Логирование

## 🔌 Расширяемость

### Добавление нового LLM провайдера

1. Создайте модели провайдера в `api/{provider}/`
2. Создайте адаптер для конвертации между доменными и API моделями
3. Реализуйте интерфейс `LlmProvider`
4. Добавьте тип провайдера в `ProviderFactory`

Пример:
```kotlin
// api/openai/OpenAIProvider.kt
class OpenAIProvider(config: AppConfig) : LlmProvider {
    override suspend fun sendRequest(request: ChatRequest): ChatResponse {
        // Реализация
    }
}
```

### Добавление нового фронтенда

1. Реализуйте интерфейс `Frontend`
2. Используйте `ConversationManager` для бизнес-логики
3. Обновите `Main.kt` для использования нового фронтенда

Пример:
```kotlin
// frontend/http/HttpFrontend.kt
class HttpFrontend(config: AppConfig) : Frontend {
    override suspend fun start(conversationManager: ConversationManager) {
        // Реализация HTTP сервера
    }
}
```

### Персистентное хранение истории

Приложение поддерживает сохранение истории диалога между перезапусками. Доступны два типа хранилищ:

- **JSON** (по умолчанию) - хранит историю в файле `ailearn.history.json`
- **SQLite** - хранит историю в базе данных `ailearn.history.db`

История автоматически сохраняется после каждого сообщения и загружается при запуске приложения. Если история была суммаризована, она сохраняется в суммаризованном виде.

**Примеры конфигурации:**

```bash
# Использование JSON хранилища (по умолчанию)
export AILEARN_MEMORY_STORE_TYPE=json
export AILEARN_MEMORY_STORE_PATH=/path/to/history.json

# Использование SQLite хранилища
export AILEARN_MEMORY_STORE_TYPE=sqlite
export AILEARN_MEMORY_STORE_PATH=/path/to/history.db
```

Или в файле `ailearn.config.properties`:
```properties
memory.store.type=sqlite
memory.store.path=ailearn.history.db
```

**Важно:** Файлы истории (`*.history.json`, `*.history.db`) автоматически исключены из git через `.gitignore`.

## 🔧 MCP интеграция и вызов инструментов

### Обзор

Приложение интегрируется с **MCP (Model Context Protocol)** серверами для поддержки вызова инструментов. Поскольку Perplexity API не поддерживает функцию вызова инструментов нативно, была реализована кастомная система вызова инструментов.

### Как это работает

1. **Пользователь отправляет сообщение** → Сообщение обрабатывается `ConversationManager`
2. **Активируется вызов инструментов** → Если доступен MCP сервис, `ToolCallingHandler` управляет процессом
3. **Системный промпт расширяется** → Доступные инструменты из MCP серверов описываются LLM
4. **LLM отвечает** → Может включать запросы инструментов в JSON формате: `{"tool": "tool_name", "arguments": {...}}`
5. **Инструменты выполняются** → `ToolRequestParser` извлекает вызовы инструментов, `McpService` выполняет их
6. **Результаты обрабатываются** → Результаты инструментов отправляются обратно в LLM как сообщение пользователя
7. **Финальный ответ** → LLM обрабатывает результаты инструментов и предоставляет финальный ответ

### Форматы запросов инструментов

Парсер поддерживает несколько форматов:
- Одиночный объект: `{"tool": "tool_name", "arguments": {"param": "value"}}`
- Массив: `[{"tool": "tool1", "arguments": {...}}]`
- Markdown блоки кода: `` ```json {"tool": "tool_name", "arguments": {...}} ``` ``
- Встроенные паттерны: `CALL_TOOL: tool_name({"arg": "value"})`

### Конфигурация MCP серверов

MCP серверы настраиваются в `api/mcp/McpConfig.kt`. Вы можете настроить несколько серверов с разными типами транспорта:

```kotlin
val servers: List<McpServerConfig> = listOf(
    McpServerConfig(
        id = "sse-server",
        baseUrl = "http://127.0.0.1:3002/sse",
        transportType = McpTransportType.SSE,
        requestTimeoutMillis = 15_000L
    ),
    McpServerConfig(
        id = "http-server",
        baseUrl = "http://127.0.0.1:8000/mcp",
        transportType = McpTransportType.STREAMABLE_HTTP,
        requestTimeoutMillis = 15_000L
    )
)
```

**Типы транспорта**:
- `SSE`: Server-Sent Events транспорт (по умолчанию, для обратной совместимости)
- `STREAMABLE_HTTP`: Streamable HTTP транспорт

**Ключевые возможности**:
- Несколько серверов могут работать одновременно с разными типами транспорта
- Инструменты из всех серверов автоматически агрегируются
- Вызовы инструментов автоматически маршрутизируются в сервер, где есть запрошенный инструмент
- Если инструмент существует в нескольких серверах, они пробуются по порядку до первого успешного результата

### Пример использования

```
Ввод: Вызови инструмент pokeapi_search_pokemon и выведи мне список покемонов

**Список первых 20 покемонов из PokeAPI:**
- bulbasaur
- ivysaur
- venusaur
...
```

Приложение автоматически:
1. Определяет запрос инструмента в ответе LLM
2. Выполняет инструмент через MCP сервер
3. Получает результаты
4. Отправляет результаты обратно в LLM
5. Показывает финальный ответ пользователю

## 🔒 Безопасность

- API ключ можно хранить в переменных окружения (рекомендуется для продакшена)
- Файлы `gradle.properties` и `ailearn.config.properties` исключены из git
- Не коммитьте файлы с API ключами в репозиторий
- Для продакшена рекомендуется использовать секретные менеджеры

## 🐛 Решение проблем

### Ошибка: "API key is not configured"

Убедитесь, что API ключ настроен одним из способов:
1. Установлена переменная окружения `AILEARN_API_KEY`
2. Создан файл `ailearn.config.properties` с `api.key=ваш_ключ`
3. В `gradle.properties` указан `perplexityApiKey=ваш_ключ` и проект пересобран

### Ошибка подключения к API

- Проверьте интернет-соединение
- Убедитесь, что API ключ действителен
- Проверьте правильность URL API (по умолчанию: `https://api.perplexity.ai/chat/completions`)
- Убедитесь, что у вас есть доступ к выбранному API провайдеру

### Ошибка: "Request timeout has expired"

- Увеличьте таймаут через переменную `AILEARN_REQUEST_TIMEOUT_MILLIS` (по умолчанию 60000 мс)
- Проверьте стабильность интернет-соединения
- Убедитесь, что API доступен

### Проблемы с историей сообщений

- История сообщений управляется через `AILEARN_USE_MESSAGE_HISTORY` (по умолчанию `true`)
- Если история отключена, каждый запрос отправляется независимо
- Используйте `sendIndependentRequest()` для изолированных запросов без истории

### Автоматическая суммаризация диалога

Приложение автоматически сжимает историю диалога, когда количество токенов в предыдущем ответе AI превышает настроенный порог (`AILEARN_SUMMARIZATION_TOKEN_THRESHOLD`, по умолчанию 600).

**Как это работает:**
1. После каждого ответа AI проверяется количество использованных токенов
2. Если `usage.totalTokens` превышает порог, запускается процесс суммаризации
3. Пользователь получает уведомление о начале суммаризации
4. Отправляется запрос к AI для создания краткого резюме диалога
5. История заменяется на системный промпт и краткое резюме
6. Новый запрос пользователя отправляется с обновленной историей

**Настройка суммаризации:**
- `summarization.token.threshold` - порог токенов (по умолчанию: 600)
- `summarization.model` - модель для суммаризации (по умолчанию: "sonar")
- `summarization.max.tokens` - максимум токенов для запроса суммаризации
- `summarization.temperature` - температура для суммаризации
- `summarization.system.prompt` - системный промпт для суммаризации
- `summarization.prompt` - инструкция для AI о том, как суммировать диалог

## 📊 Как это работает

1. **Инициализация**: Загружается конфигурация из различных источников
2. **Создание провайдера**: Фабрика создает экземпляр LLM провайдера (Perplexity по умолчанию)
3. **Менеджер разговора**: Создается менеджер для управления состоянием диалога
4. **Фронтенд**: Запускается интерфейс пользователя (CLI по умолчанию)
5. **Обработка запросов**: Пользователь вводит сообщения, которые обрабатываются через менеджер разговора
6. **Автоматическая суммаризация**: При превышении порога токенов история диалога автоматически сжимается
7. **Отображение**: Ответы и статистика токенов отображаются пользователю

## 🧪 Тестирование

Архитектура поддерживает легкое тестирование:

- **Unit тесты**: Тестирование бизнес-логики с моками провайдеров
- **Integration тесты**: Тестирование реализаций провайдеров
- **E2E тесты**: Тестирование полного потока

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад в проект

Проект следует лучшим практикам:
- Чистая архитектура с разделением ответственности
- SOLID принципы
- Типобезопасность через sealed classes и enums
- Модульность и расширяемость
- Поддержка истории диалога для контекста
- Автоматическая суммаризация для управления длинными диалогами

## 🔍 RAG System

Приложение включает систему RAG (Retrieval-Augmented Generation) для контекстно-зависимых ответов на основе вашей базы знаний.

**Быстрый старт:**
```
# Создать индекс
/index

# Включить RAG режим (все запросы будут использовать базу знаний)
/rag

# Теперь задавайте вопросы как обычно
Что такое RAG?
Как работает индексация?

# Выключить RAG режим
/rag

# Или используйте одноразовые запросы без включения режима
/ask Что такое RAG?
```

Полная документация по RAG: [RAG_DESCRIPTION.md](RAG_DESCRIPTION.md)

## 📚 Дополнительная документация

- [ARCHITECTURE.md](ARCHITECTURE.md) - Подробное описание архитектуры, включая MCP интеграцию
- [PROJECT_DESCRIPTION.md](PROJECT_DESCRIPTION.md) - Описание проекта и компонентов
- [RAG_DESCRIPTION.md](RAG_DESCRIPTION.md) - Полная документация по системе RAG (индексация, поиск, re-ranking)
- [HISTORY_PERSISTENCE_GUIDE.md](HISTORY_PERSISTENCE_GUIDE.md) - Руководство по управлению сохранением истории диалогов
