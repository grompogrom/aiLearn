# AI Learn - Терминальная программа для работы с AI

Терминальное приложение на Kotlin для интерактивной работы с Perplexity AI API. Программа принимает ввод от пользователя, отправляет запросы в API и автоматически обрабатывает JSON-ответы, выводя структурированную информацию в консоль.

## 🚀 Возможности

- **Интерактивный режим работы** в терминале
- **Интеграция с Perplexity AI API** через Ktor HTTP клиент
- **Автоматическая обработка ответов** в формате JSON
- **Автоматические повторные запросы** при невалидном JSON (до 3 попыток)
- **Типизированная обработка ошибок** через sealed classes
- **Безопасное хранение API ключа** в конфигурационном файле
- **Идиоматичный Kotlin код** с использованием современных практик

## 📋 Требования

- **Java 21** или выше
- **Gradle 8.0** или выше

## 🔧 Установка

1. Клонируйте репозиторий или скачайте проект
2. Перейдите в директорию проекта:
   ```bash
   cd aiLearn
   ```

3. Создайте файл `gradle.properties` в корне проекта (если его нет):
   ```bash
   touch gradle.properties
   ```

4. Добавьте в `gradle.properties` ваш API ключ от Perplexity:
   ```properties
   kotlin.code.style=official
   perplexityApiKey=ваш_api_ключ_здесь
   ```

   > **Важно:** Файл `gradle.properties` уже добавлен в `.gitignore` и не будет попадать в репозиторий.

## 🏃 Запуск

### Через Gradle

```bash
./gradlew run
```

### Сборка JAR файла

```bash
./gradlew build
```

Запуск собранного JAR:
```bash
java -jar build/libs/aiLearn-1.0-SNAPSHOT.jar
```

## 💻 Использование

После запуска программы:

1. Вы увидите приветственное сообщение и информацию об API URL
2. Введите ваш вопрос или запрос
3. Нажмите Enter для отправки
4. Программа отобразит структурированный ответ от AI:
   - **Ответ** - прямой ответ на ваш вопрос
   - **Рекомендации** - дополнительные рекомендации по теме
5. Для выхода введите `exit` или `quit`

### Пример использования

```
=== Терминальная программа для работы с AI ===
AI URL: https://api.perplexity.ai/chat/completions
Введите 'exit' или 'quit' для выхода

Теперь вводите данные для отправки в API:

Ввод: Что такое искусственный интеллект?

Ответ:
Искусственный интеллект (ИИ) - это область компьютерных наук, которая занимается созданием систем, способных выполнять задачи, обычно требующие человеческого интеллекта.

Рекомендации:
Для изучения ИИ рекомендуется начать с основ машинного обучения, изучить Python и библиотеки типа TensorFlow или PyTorch. Также полезно понимать математические основы: линейную алгебру, статистику и теорию вероятностей.

Ввод: exit
Завершение работы...
Программа завершена.
```

### Автоматические повторные запросы

Если API вернул ответ не в формате JSON, программа автоматически выполнит повторный запрос (до 3 попыток). При успешном получении валидного JSON после ретраев, вы увидите сообщение:

```
(Потребовалось 2 ретрая для получения валидного ответа)
```

## 📁 Структура проекта

```
aiLearn/
├── src/
│   └── main/
│       └── kotlin/
│           ├── Main.kt                    # Точка входа приложения
│           ├── ApiClient.kt               # Клиент для работы с API
│           ├── ApiException.kt            # Типизированные исключения
│           ├── Config.kt                   # Конфигурация приложения
│           ├── models/
│           │   ├── AiResponse.kt           # Модель ответа AI
│           │   ├── ChatRequest.kt         # Модели запросов
│           │   ├── ChatResponse.kt        # Модели ответов API
│           │   └── MessageRole.kt        # Enum ролей сообщений
│           └── utils/
│               └── StringExtensions.kt    # Extension functions
├── build.gradle.kts                       # Конфигурация сборки
├── gradle.properties                      # Конфигурация (API ключ)
└── README.md                              # Документация
```

## 🛠 Технологии

- **Kotlin 2.2.10** - Язык программирования
- **Ktor 2.3.12** - HTTP клиент
- **Kotlinx Serialization** - Сериализация JSON
- **Kotlin Coroutines** - Асинхронное программирование
- **Gradle** - Система сборки
- **SLF4J Simple** - Логирование

## ⚙️ Конфигурация

Все настройки находятся в файле `Config.kt`:

### Изменение модели AI

```kotlin
const val MODEL = "sonar"  // или "sonar-pro", "sonar-reasoning" и т.д.
```

### Изменение максимального количества токенов

```kotlin
const val MAX_TOKENS = 1000  // Измените на нужное значение
```

### Изменение количества повторных попыток

```kotlin
const val MAX_RETRIES = 3  // Количество попыток при невалидном JSON
```

### Изменение system prompt

```kotlin
const val SYSTEM_PROMPT = """..."""  // Промпт для настройки поведения AI
```

## 🏗 Архитектура

Проект использует современные практики Kotlin:

- **Sealed Classes** для типизированной обработки ошибок (`ApiException`)
- **Enum Classes** для типобезопасности (`MessageRole`)
- **Extension Functions** для переиспользования логики (`StringExtensions`)
- **AutoCloseable** для автоматического управления ресурсами
- **Suspend Functions** для асинхронных операций
- **Factory Methods** для удобного создания объектов

## 📊 Формат ответа

AI возвращает ответы в формате JSON с двумя полями:

```json
{
  "answer": "Прямой ответ на вопрос пользователя",
  "recomendation": "Дополнительные рекомендации"
}
```

Программа автоматически парсит этот JSON и форматирует вывод для удобного чтения.

## 🔒 Безопасность

- API ключ хранится в `gradle.properties`, который исключен из git
- Не коммитьте файл `gradle.properties` в репозиторий
- Для продакшена рекомендуется использовать переменные окружения или секретные менеджеры
- API ключ передается через BuildConfig, генерируемый на этапе сборки

## 🐛 Решение проблем

### Ошибка: "API ключ не найден"

Убедитесь, что:
1. Файл `gradle.properties` существует в корне проекта
2. В файле есть строка `perplexityApiKey=ваш_ключ`
3. Ключ указан без пробелов и кавычек
4. Вы пересобрали проект после добавления ключа: `./gradlew build`

### Ошибка подключения к API

- Проверьте интернет-соединение
- Убедитесь, что API ключ действителен
- Проверьте, что у вас есть доступ к Perplexity AI API
- Убедитесь, что URL API правильный в `Config.kt`

### Ошибка: "Не удалось получить ответ в формате JSON"

- Программа автоматически делает до 3 повторных попыток
- Если после всех попыток ответ не в формате JSON, проверьте:
  - Правильность `SYSTEM_PROMPT` в `Config.kt`
  - Достаточно ли `MAX_TOKENS` для полного ответа
  - Работает ли API корректно

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад в проект

Проект использует идиоматичный Kotlin код и следует лучшим практикам:
- Чистая архитектура с разделением ответственности
- Типобезопасность через sealed classes и enums
- Переиспользование кода через extension functions
- Автоматическое управление ресурсами
