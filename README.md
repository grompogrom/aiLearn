# AI Learn - Генератор технических заданий (ТЗ)

Терминальное приложение на Kotlin для автоматического создания технических заданий (ТЗ) с помощью Perplexity AI API. Программа ведет интерактивный диалог с пользователем, задает уточняющие вопросы и формирует структурированное ТЗ.

## 🚀 Возможности

- **Интерактивный диалог** для сбора информации о проекте
- **Автоматическое формирование ТЗ** на основе ответов пользователя
- **Умная нумерация ответов** - программа автоматически отслеживает, на какой вопрос отвечает пользователь
- **История сообщений** - ассистент помнит весь контекст диалога
- **Автоматическое завершение** - диалог завершается после формирования ТЗ
- **Интеграция с Perplexity AI API** через Ktor HTTP клиент
- **Типизированная обработка ошибок** через sealed classes
- **Безопасное хранение API ключа** в конфигурационном файле
- **Идиоматичный Kotlin код** с использованием современных практик

## 📋 Требования

- **Java 21** или выше
- **Gradle 8.0** или выше

## 🔧 Установка

1. Клонируйте репозиторий или скачайте проект
2. Перейдите в директорию проекта:
   ```bash
   cd aiLearn
   ```

3. Создайте файл `gradle.properties` в корне проекта (если его нет):
   ```bash
   touch gradle.properties
   ```

4. Добавьте в `gradle.properties` ваш API ключ от Perplexity:
   ```properties
   kotlin.code.style=official
   perplexityApiKey=ваш_api_ключ_здесь
   ```

   > **Важно:** Файл `gradle.properties` уже добавлен в `.gitignore` и не будет попадать в репозиторий.

## 🏃 Запуск

### Через Gradle

```bash
./gradlew run
```

### Сборка JAR файла

```bash
./gradlew build
```

Запуск собранного JAR:
```bash
java -jar build/libs/aiLearn-1.0-SNAPSHOT.jar
```

## 💻 Использование

После запуска программы:

1. Вы увидите приветственное сообщение
2. Опишите, что вы хотите создать
3. Ассистент задаст примерно 5 уточняющих вопросов для уточнения деталей
4. Отвечайте на вопросы - ваши ответы автоматически нумеруются
5. После сбора информации ассистент сформирует компактное ТЗ (около 4 пунктов)
6. Диалог завершится автоматически после формирования ТЗ
7. Для выхода в любой момент введите `exit` или `quit`

### Пример использования

```
=== Генератор технических заданий (ТЗ) ===
Опишите, что вы хотите создать, и ассистент поможет составить ТЗ
Ассистент задаст примерно 5 уточняющих вопросов для уточнения деталей
После формирования ТЗ диалог завершится автоматически

Введите 'exit' или 'quit' для выхода в любой момент

Ввод: Хочу создать мобильное приложение для заказа еды

Какая целевая платформа для мобильного приложения?

Ввод: iOS и Android

Какой функционал должен быть в приложении?

Ввод: Просмотр меню, корзина, оформление заказа, оплата

Какой способ оплаты планируется?

Ввод: Карта и наличные при доставке

Нужна ли система рейтингов и отзывов?

Ввод: Да, с возможностью оставлять отзывы

[ТЗ формируется...]

=== Техническое задание ===

1. Платформы: iOS и Android (нативные приложения)
2. Основной функционал: каталог блюд, корзина, оформление заказа, интеграция платежных систем
3. Способы оплаты: банковская карта, наличные при доставке
4. Дополнительно: система рейтингов и отзывов для ресторанов и блюд

###END###

=== Диалог завершен ===
Программа завершена.
```

## 📁 Структура проекта

```
aiLearn/
├── src/
│   └── main/
│       └── kotlin/
│           ├── Main.kt                    # Точка входа приложения
│           ├── ApiClient.kt               # Клиент для работы с API
│           ├── ApiException.kt            # Типизированные исключения
│           ├── Config.kt                   # Конфигурация приложения
│           ├── models/
│           │   ├── AiResponse.kt           # Модель ответа AI (не используется)
│           │   ├── ChatRequest.kt         # Модели запросов
│           │   ├── ChatResponse.kt        # Модели ответов API
│           │   └── MessageRole.kt         # Enum ролей сообщений
│           └── utils/
│               └── StringExtensions.kt    # Extension functions
├── build.gradle.kts                       # Конфигурация сборки
├── gradle.properties                      # Конфигурация (API ключ)
└── README.md                              # Документация
```

## 🛠 Технологии

- **Kotlin 2.2.10** - Язык программирования
- **Ktor 2.3.12** - HTTP клиент
- **Kotlinx Serialization** - Сериализация JSON
- **Kotlin Coroutines** - Асинхронное программирование
- **Gradle** - Система сборки
- **SLF4J Simple** - Логирование

## ⚙️ Конфигурация

Все настройки находятся в файле `Config.kt`:

### Изменение модели AI

```kotlin
const val MODEL = "sonar"  // или "sonar-pro", "sonar-reasoning" и т.д.
```

### Изменение максимального количества токенов

```kotlin
const val MAX_TOKENS = 1000  // Измените на нужное значение
```

### Изменение system prompt

```kotlin
val SYSTEM_PROMPT = """..."""  // Промпт для настройки поведения AI
```

В `SYSTEM_PROMPT` можно настроить:
- Количество уточняющих вопросов (по умолчанию ~5)
- Формат и структуру ТЗ
- Требования к компактности (по умолчанию ~4 пункта)
- Стиль вопросов и ответов

## 🏗 Архитектура

Проект использует современные практики Kotlin:

- **Sealed Classes** для типизированной обработки ошибок (`ApiException`)
- **Enum Classes** для типобезопасности (`MessageRole`)
- **Extension Functions** для переиспользования логики (`StringExtensions`)
- **AutoCloseable** для автоматического управления ресурсами
- **Suspend Functions** для асинхронных операций
- **Factory Methods** для удобного создания объектов
- **История сообщений** для поддержания контекста диалога

## 📊 Как это работает

1. **Начало диалога**: Пользователь описывает задачу
2. **Уточняющие вопросы**: Ассистент задает примерно 5 вопросов для уточнения деталей
3. **Нумерация ответов**: Каждый ответ пользователя автоматически помечается номером вопроса
4. **Формирование ТЗ**: После сбора информации ассистент создает компактное ТЗ (около 4 пунктов)
5. **Завершение**: ТЗ заканчивается маркером `###END###`, после чего диалог автоматически завершается

## 🔒 Безопасность

- API ключ хранится в `gradle.properties`, который исключен из git
- Не коммитьте файл `gradle.properties` в репозиторий
- Для продакшена рекомендуется использовать переменные окружения или секретные менеджеры
- API ключ передается через BuildConfig, генерируемый на этапе сборки

## 🐛 Решение проблем

### Ошибка: "API ключ не найден"

Убедитесь, что:
1. Файл `gradle.properties` существует в корне проекта
2. В файле есть строка `perplexityApiKey=ваш_ключ`
3. Ключ указан без пробелов и кавычек
4. Вы пересобрали проект после добавления ключа: `./gradlew build`

### Ошибка подключения к API

- Проверьте интернет-соединение
- Убедитесь, что API ключ действителен
- Проверьте, что у вас есть доступ к Perplexity AI API
- Убедитесь, что URL API правильный в `Config.kt`

### Ошибка: "Request timeout has expired"

- Увеличьте таймаут в `ApiClient.kt` (по умолчанию 60 секунд)
- Проверьте стабильность интернет-соединения
- Убедитесь, что API доступен

### Ошибка: "Field 'choices' is required"

- Это означает, что API вернул ошибку вместо нормального ответа
- Программа автоматически покажет тело ответа от API
- Проверьте правильность API ключа и доступность сервиса

### Ассистент задает слишком много/мало вопросов

- Настройте количество вопросов в `SYSTEM_PROMPT` в `Config.kt`
- Измените строку: "aim for approximately 5 questions" на нужное значение

### ТЗ получается слишком длинным

- В `SYSTEM_PROMPT` уже указано требование о компактности (~4 пункта)
- При необходимости усильте требования в промпте

## 📝 Лицензия

Этот проект создан в образовательных целях.

## 🤝 Вклад в проект

Проект использует идиоматичный Kotlin код и следует лучшим практикам:
- Чистая архитектура с разделением ответственности
- Типобезопасность через sealed classes и enums
- Переиспользование кода через extension functions
- Автоматическое управление ресурсами
- Поддержка истории диалога для контекста
