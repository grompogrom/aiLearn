# AI Code Review - Документация

## 📋 Обзор

Фича **AI Code Review** позволяет автоматически анализировать merge requests (MR) и pull requests (PR) с помощью AI. Система получает diff через GitHub API, обогащает его контекстом проекта из RAG базы знаний и отправляет на анализ в LLM с специальным системным промптом для code review.

## 🚀 Возможности

- ✅ **Автоматический анализ MR/PR** через GitHub API
- ✅ **Контекст проекта** из RAG базы знаний
- ✅ **Структурированный review** с оценкой качества кода, архитектуры, багов и производительности
- ✅ **Автоматическая отправка review** как комментария в MR/PR через GitHub API
- ✅ **Поддержка GitHub и GitLab** ссылок
- ✅ **Настраиваемый системный промпт** для review

## 📖 Использование

### Базовое использование

```bash
/review https://github.com/owner/repo/pull/123
```

или для GitLab:

```bash
/review https://gitlab.com/owner/repo/-/merge_requests/123
```

### Что происходит при выполнении команды

1. **Парсинг ссылки** → извлекаются owner, repo и номер PR/MR
2. **Получение diff** → через GitHub REST API с использованием токена
3. **Получение контекста проекта** → через RAG систему (опционально, если RAG доступен)
4. **AI Review** → LLM анализирует diff с контекстом проекта
5. **Вывод результата** → структурированный review с рекомендациями выводится в консоль
6. **Отправка комментария** → review автоматически отправляется как комментарий в MR/PR через GitHub API

### Пример вывода

```
🔍 Получение диффа MR через GitHub API...
📚 Получение контекста проекта через RAG...
🤖 Выполнение AI review...

=== AI CODE REVIEW ===

**Summary**: 
This PR introduces a new feature for user authentication with JWT tokens.

**Positive Aspects**:
- Clean separation of concerns with dedicated AuthService
- Proper error handling with custom exceptions
- Good test coverage for authentication flow

**Issues Found**:
1. [Critical] Missing input validation in login endpoint - potential SQL injection risk
2. [High] JWT secret key is hardcoded - should use environment variables
3. [Medium] No rate limiting on login attempts - security concern
4. [Low] Missing JSDoc comments for public methods

**Suggestions**:
- Extract JWT secret to environment configuration
- Add input sanitization before database queries
- Implement rate limiting middleware
- Add comprehensive error logging

**Overall Assessment**: Request Changes
The PR needs security improvements before merging.

=== END OF REVIEW ===

💬 Отправка review как комментария в MR...
✅ Review успешно отправлен как комментарий в MR!
```

## ⚙️ Конфигурация

### GitHub Token

Для работы фичи необходим GitHub Personal Access Token с правами доступа к репозиториям.

**Вариант 1: Переменная окружения (рекомендуется)**

```bash
export AILEARN_GITHUB_TOKEN=ghp_your_token_here
```

**Вариант 2: Файл конфигурации**

Создайте или отредактируйте `ailearn.config.properties`:

```properties
github.token=ghp_your_token_here
```

### Системный промпт для Review

По умолчанию используется промпт, который настраивает LLM как эксперта по code review. Вы можете настроить свой промпт:

**Переменная окружения:**

```bash
export AILEARN_AI_REVIEW_SYSTEM_PROMPT="Your custom review prompt here"
```

**Файл конфигурации:**

```properties
ai.review.system.prompt=Your custom review prompt here
```

### Дефолтный системный промпт

```
You are an expert code reviewer. Your task is to review a merge request (MR) and provide constructive feedback.

Guidelines for your review:
1. **Code Quality**: Check for code quality, readability, maintainability, and adherence to best practices
2. **Architecture**: Evaluate if the changes align with the project's architecture and design patterns
3. **Bugs & Issues**: Identify potential bugs, edge cases, and security vulnerabilities
4. **Performance**: Consider performance implications of the changes
5. **Testing**: Check if adequate tests are included or needed
6. **Documentation**: Verify if documentation is updated appropriately

Provide your review in a structured format:
- **Summary**: Brief overview of the changes
- **Positive Aspects**: What was done well
- **Issues Found**: List of issues, bugs, or concerns with severity (Critical/High/Medium/Low)
- **Suggestions**: Recommendations for improvement
- **Overall Assessment**: Final verdict (Approve/Request Changes/Needs Discussion)

Be constructive, specific, and provide actionable feedback. Reference specific lines or sections when possible.
```

## 🏗 Архитектура

### Компоненты

#### 1. **Core Layer** (`core/github/`)

- **`GithubUtils.kt`** - утилиты для парсинга ссылок на MR/PR
  - `parseMrLink(link: String)` - извлекает owner, repo, prNumber из ссылки
  - Поддерживает GitHub и GitLab форматы

#### 2. **API Layer** (`api/github/`)

- **`GithubClient.kt`** - клиент для GitHub REST API
  - `getPullRequestDiff(owner, repo, prNumber)` - получает diff через API
  - `postComment(owner, repo, prNumber, commentBody)` - отправляет комментарий в PR
  - Использует Ktor HttpClient
  - Реализует `AutoCloseable` для управления ресурсами

#### 3. **Core Layer** (`core/conversation/`)

- **`ConversationManager.kt`** - метод `performReview()`
  - Объединяет diff и RAG контекст
  - Отправляет запрос в LLM с кастомным системным промптом
  - Возвращает структурированный review

#### 4. **Frontend Layer** (`frontend/cli/`)

- **`CliFrontend.kt`** - обработка команды `/review`
  - Парсит пользовательский ввод
  - Координирует получение diff и RAG контекста
  - Вызывает `performReview()` и выводит результат
  - Автоматически отправляет review как комментарий в PR через `GithubClient.postComment()`

### Поток данных

```
User Input (/review <link>)
    ↓
CliFrontend.handleReviewCommand()
    ↓
GithubUtils.parseMrLink() → (owner, repo, prNumber)
    ↓
GithubClient.getPullRequestDiff() → diff
    ↓
RagQueryService.query() → project context (optional)
    ↓
ConversationManager.performReview(diff, context)
    ↓
LLM Provider → structured review
    ↓
User Output (console)
    ↓
GithubClient.postComment() → comment posted to PR
```

## 📝 Примеры использования

### Пример 1: Простой review

```bash
/review https://github.com/myorg/myrepo/pull/42
```

### Пример 2: Review с RAG контекстом

Если RAG индекс создан (`/index`), система автоматически добавит контекст проекта:

```bash
# Сначала создайте индекс
/index

# Затем выполните review
/review https://github.com/myorg/myrepo/pull/42
```

Review будет содержать информацию об архитектуре проекта, что улучшит качество анализа.

### Пример 3: Настройка промпта для специфичных требований

Если ваш проект использует специфичные стандарты кодирования:

```properties
ai.review.system.prompt=You are a code reviewer for a Kotlin project following Clean Architecture principles. Focus on:
- SOLID principles compliance
- Kotlin idioms and best practices
- Architecture layer separation
- Error handling patterns
...
```

## 🔧 Требования

### Обязательные

- ✅ GitHub Personal Access Token с правами `repo` scope
- ✅ LLM провайдер настроен (Perplexity или другой)
- ✅ Доступ к репозиторию через GitHub API

### Опциональные

- 📚 RAG индекс для контекста проекта (улучшает качество review)
- ⚙️ Кастомный системный промпт для специфичных требований

## 🐛 Решение проблем

### Ошибка: "GitHub token is not configured"

**Решение**: Установите переменную окружения или добавьте в конфиг файл:

```bash
export AILEARN_GITHUB_TOKEN=ghp_your_token_here
```

### Ошибка: "GitHub API returned error 404"

**Возможные причины**:
- Репозиторий не существует или недоступен
- Токен не имеет прав доступа к репозиторию
- Неверный формат ссылки

**Решение**: 
- Проверьте права токена (нужен `repo` scope)
- Убедитесь, что ссылка на MR указана правильно
- Проверьте, что репозиторий существует и доступен

### Ошибка: "GitHub API returned error 401"

**Причина**: Неверный или истекший токен

**Решение**: 
- Создайте новый Personal Access Token
- Убедитесь, что токен имеет необходимые права (`repo` scope)

### Ошибка: "Failed to get PR diff from GitHub API"

**Возможные причины**:
- Проблемы с сетью
- Таймаут запроса
- GitHub API недоступен

**Решение**:
- Проверьте интернет-соединение
- Увеличьте таймаут в конфигурации: `AILEARN_REQUEST_TIMEOUT_MILLIS=120000`
- Повторите попытку позже

### RAG контекст не получен

**Причина**: RAG индекс не создан или Ollama недоступна

**Решение**: 
- Это не критично - review будет выполнен без контекста проекта
- Для улучшения качества создайте индекс: `/index`
- Убедитесь, что Ollama запущена

### Ошибка: "Не удалось отправить комментарий в MR"

**Возможные причины**:
- Токен не имеет прав на запись комментариев в репозиторий
- Репозиторий недоступен или PR закрыт
- Проблемы с сетью или GitHub API

**Решение**:
- Проверьте права токена (нужен `repo` scope для приватных репозиториев или `public_repo` для публичных)
- Убедитесь, что PR открыт и доступен
- Review все равно будет выведен в консоль, даже если отправка комментария не удалась

## 📊 Формат Review

Review структурирован следующим образом:

1. **Summary** - краткий обзор изменений
2. **Positive Aspects** - что сделано хорошо
3. **Issues Found** - найденные проблемы с уровнем критичности:
   - Critical - критичные проблемы, блокирующие merge
   - High - важные проблемы, требующие исправления
   - Medium - проблемы, которые желательно исправить
   - Low - мелкие замечания и предложения
4. **Suggestions** - рекомендации по улучшению
5. **Overall Assessment** - итоговая оценка:
   - Approve - можно мержить
   - Request Changes - требуются изменения
   - Needs Discussion - требуется обсуждение

## 🔐 Безопасность

- **GitHub Token**: Храните токен в переменных окружения или конфиг файле (не коммитьте в репозиторий!)
- **Права доступа**: Используйте токен с минимально необходимыми правами (`repo` scope)
- **Конфиденциальность**: Diff и review могут содержать чувствительную информацию - будьте осторожны при логировании

## 🎯 Лучшие практики

1. **Используйте RAG контекст**: Создайте индекс проекта для более качественного review
2. **Настройте промпт**: Адаптируйте системный промпт под специфику вашего проекта
3. **Проверяйте токен**: Убедитесь, что токен имеет актуальные права доступа
4. **Комбинируйте с ручным review**: AI review дополняет, но не заменяет человеческий review

## 📚 Связанная документация

- [README.md](README.md) - общая документация проекта
- [ARCHITECTURE.md](ARCHITECTURE.md) - описание архитектуры
- [RAG_DESCRIPTION.md](RAG_DESCRIPTION.md) - документация по RAG системе

## 🔄 История изменений

- **v1.1** (2025-01-XX): Добавлена автоматическая отправка review как комментария
  - Автоматическая отправка результата review как комментария в MR/PR через GitHub API
  - Улучшена обратная связь пользователю о статусе отправки комментария

- **v1.0** (2025-01-XX): Первая версия фичи AI Code Review
  - Поддержка GitHub и GitLab ссылок
  - Интеграция с GitHub API
  - Интеграция с RAG системой
  - Настраиваемый системный промпт
